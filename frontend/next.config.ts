import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
    images: {
        domains: ['scontent.cdninstagram.com', 'instagram.fpoz3-1.fna.fbcdn.net', 'drive.usercontent.google.com'],
    }
};

export default nextConfig;
